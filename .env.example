# HTTP port for accessing phpMyAdmin in your browser
APP_PORT=6868

# Cookie encryption secret (32+ random chars). Generate: openssl rand -base64 32
PMA_BLOWFISH_SECRET=CHANGE_ME_replace_with_a_32_plus_character_random_string

# All server definitions (SSH tunnels + direct DBs) are in config/servers.conf
